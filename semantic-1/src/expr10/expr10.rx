// Error: Invalid field access expression - missing field name after dot
struct Person {
    name: String,
    age: i32,
}

fn main() {
    let p = Person { name: "Alice", age: 30 };
    let x = p.;  // Missing field name after dot
}
