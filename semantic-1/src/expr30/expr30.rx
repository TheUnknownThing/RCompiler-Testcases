fn main() {
    let base = 5;
    let exponent = 3;
    let result = loop {
        if exponent == 0 {
            break 1;
        } else if exponent == 1 {
            break base;
        } else {
            break base * base;
        }
    };
    let arr = [result, result * 2, result * 3];
    let sum = arr[0] + arr[1] + arr[2];
    let final_result = (sum + 10) / ;
    printInt(final_result);
}
