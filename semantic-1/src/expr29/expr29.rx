fn fibonacci(n: i32) -> i32 {
    if n <= 1 {
        return n;
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
}

fn main() {
    let input = readInt();
    let fib_result = fibonacci(input);
    let processed = {
        let temp = fib_result + 10;
        temp * 2
    };
    let condition = processed > 0;
    if condition {
        printInt(processed);
    }
    let array_val = [processed][0;
    exit(0);
}
