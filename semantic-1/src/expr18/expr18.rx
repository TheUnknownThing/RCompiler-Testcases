struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 1, y: };
    let sum = p.x + p.y;
    if sum > 0 {
        printInt(sum);
    } else {
        printInt(0);
    }
    exit(0);
}
