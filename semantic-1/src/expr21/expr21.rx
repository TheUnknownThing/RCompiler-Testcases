fn main() {
    let a: i32 = 10;
    let b: i32 = 20;
    let c: i32 = {
        let temp: i32 = a + b;
        temp * 2
    }
    if c > 50 {
        printInt(c);
    } else {
        printInt(-c);
    }
    exit(0);
}
